{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "shopify": "shopify",
    "dev": "shopify app dev && astro dev",
    "start": "shopify app dev && astro dev",
    "build": "shopify app build && astro build",
    "preview": "shopify app dev && astro preview"
  },
  "devDependencies": {
    "@astrojs/preact": "^1.0.2",
    "@astrojs/sitemap": "^1.0.0",
    "@netlify/netlify-marketing-tokens": "^1.1.0",
    "astro": "^2.0.0",
    "@shopify/app": "^3.0.0",
    "@shopify/cli": "^3.0.0",
    "stripe": "^12.17.0"
  }
}
